# Trial data

Runtime CSV files are written to this directory and ignored by Git.

Do not commit participant or raw study data. Keep those files in controlled,
backed-up storage and publish only an intentionally de-identified sample when
appropriate.
